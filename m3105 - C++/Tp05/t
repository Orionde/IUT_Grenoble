# Fichier de test
# Les lignes de commentaires commencent par #
procedure principale()
  a = 0;
  b = 5;
  #tantque(a < 4)
  	 #a = a + 1;
  #fintantque
  #x = 0;
 # pour(a = 0 ; a < 5 ; a = a + 1)
  #  i = i + 3;
  #finpour
  #ecrire(a, b)
  x = 0;
  repeter x = x + 5 ;
  jusqua(x > 10)
finproc
# Commentaire Final
